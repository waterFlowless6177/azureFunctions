@model IEnumerable<LaserAfzar.Entities.ServiceImage>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create", new { id = ViewBag.SubServiceId, CompanyServiceId = ViewBag.CompanyServiceId })
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Title)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FileName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Title)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FileName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ServiceImageId, SubServiceId = ViewBag.SubServiceId, CompanyServiceId = ViewBag.CompanyServiceId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ServiceImageId, SubServiceId = ViewBag.SubServiceId, CompanyServiceId = ViewBag.CompanyServiceId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ServiceImageId, SubServiceId = ViewBag.SubServiceId, CompanyServiceId = ViewBag.CompanyServiceId })
        </td>
    </tr>
}

</table>

<div>
    @Html.ActionLink("Back to List", "Index", "SubServices", new { id = ViewBag.CompanyServiceId }, null)
</div>
